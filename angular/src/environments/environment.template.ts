// Copy this template, replace placeholders with your actual values in the copied file, 
// and ensure it is named environment.secret.ts to keep sensitive information secure 
// and untracked by version control.
export const environment = {
    endpoint: 'INSERT_ENDPOINT_HERE',
    apiKey: 'INSERT_API_KEY_HERE',
};